// program to print an integer
#include<stdio.h>
#include<conio.h>
void main()
{
    int number;
    // print() displays the formatted output
    printf("enter an integer: ");
    // scan() reads the formatted and store them
    scanf("%d",&number);
    // print() displays the formatted output
    printf("you entered: %d",number);
    getch();
}
